
  # Animated Login Screen

  This is a code bundle for Animated Login Screen. The original project is available at https://www.figma.com/design/ci0GT38bwtI6h54bdLdBrR/Animated-Login-Screen.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  